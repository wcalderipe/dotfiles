;;; pkg-buffer-move.el --- -*- lexical-binding: t; -*-

;;; Swap buffers easily (great combo with the hydra package).

(require 'use-package)

(use-package buffer-move
  :straight t
  :defer t)

(provide 'pkg-buffer-move)

;;; pkg-buffer-move.el ends here
