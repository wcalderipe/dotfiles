(beacon-mode 1)

;; Time, in seconds, before starting to fade the beacon.
(setq beacon-blink-delay 0.25)

;; If it is a number, the color is taken to be white or black
;; (depending on the current theme’s background) and this number is a
;; float between 0 and 1 specifing the brightness.
(setq beacon-color 0.6)

(setq beacon-size 30)
(setq beacon-blink-when-focused t)
(setq beacon-blink-when-window-scrolls t)
