- name: Set up local workstation
  hosts: local
  roles:
    - role: common
      tags:
        - common

    - role: terminal
      tags:
        - terminal

    - role: docker
      tags:
        - docker

    # TODO: Needs to be tested outside a docker container.
    - role: emacs
      tags:
        - emacs

    - role: javascript
      tags:
        - javascript

    - role: java
      tags:
        - java

    - role: apps
      tags:
        - apps

    # TODO: Needs to be tested outside a docker container.
    # - role: gnome
    #   tags:
    #     - gnome
